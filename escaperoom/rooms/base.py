from abc import abstractmethod, ABC


class BaseRoom(ABC):
    @abstractmethod
    def __init__(self):
        while False:
            yield none

    @abstractmethod
    def solve(self):
        while False:
            yield none